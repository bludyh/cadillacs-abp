// Same as in back-end. 
// Currently need to manually update this class if it changes in back-end.
// Possibly consider alternatives in future.
export enum AssignmentType {
    REPORT,
    PORTFOLIO,
    CODE
}